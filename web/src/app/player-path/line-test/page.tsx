import LineTestShell from './LineTestShell';

export const metadata = {
  title: 'The Line Test — PlayerPath',
  description:
    'A player path standard and dispersion benchmark. Measures lateral dispersion across the bag in three distance bands, placing you on a five-tier ladder anchored to college, amateur, and tour reference populations.',
};

export default function LineTestPage() {
  return <LineTestShell />;
}
