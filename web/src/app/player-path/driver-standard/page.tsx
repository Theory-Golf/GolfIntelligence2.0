import DriverStandardShell from './DriverStandardShell';

export const metadata = {
  title: 'Driver Standard — PlayerPath',
  description:
    'A periodized practice protocol for off-the-tee accuracy. Five tiers anchored to PGA Tour and USGA fairway standards. Binary scoring with optional shape mode. The standard rises with you.',
};

export default function DriverStandardPage() {
  return <DriverStandardShell />;
}
